import { PageContainer, Typography } from "@ui";

const VisioningPage = ({}) => {
  return (
    <PageContainer>
      <Typography>Visioning</Typography>
    </PageContainer>
  );
};

export default VisioningPage;
