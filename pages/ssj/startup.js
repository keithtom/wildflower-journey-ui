import { PageContainer, Typography } from "@ui";

const StartupPage = ({}) => {
  return (
    <PageContainer>
      <Typography>Startup</Typography>
    </PageContainer>
  );
};

export default StartupPage;
