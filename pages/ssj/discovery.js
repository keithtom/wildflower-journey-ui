import { PageContainer, Typography } from "@ui";

const DiscoveryPage = ({}) => {
  return (
    <PageContainer>
      <Typography>Discovery</Typography>
    </PageContainer>
  );
};

export default DiscoveryPage;
