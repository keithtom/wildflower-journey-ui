import { PageContainer, Typography } from "@ui";

const SSJ = ({}) => {
  return (
    <PageContainer>
      <Typography variant="h3" bold>
        School Startup Journey
      </Typography>
    </PageContainer>
  );
};

export default SSJ;
