import { PageContainer, Typography } from "@ui";
const PlanningPage = ({}) => {
  return (
    <PageContainer>
      <Typography>Planning</Typography>
    </PageContainer>
  );
};

export default PlanningPage;
